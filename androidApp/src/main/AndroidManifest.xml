<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      usesCleartextTraffic=false is a release gate, not a default: the GitHub API is HTTPS-only and
      there is no debug-only bypass, because a bypass in a debug manifest merges into release
      variants more often than anyone expects.
    -->
    <application
        android:name=".GithubSearchApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false">
        <!--
          A platform NoActionBar theme, not an AppCompat one: Compose draws every pixel of this
          app, so the only thing the theme decides is the window background shown for the frame
          before the first composition. Using an AppCompat theme would mean an AppCompat
          dependency for that one frame.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GithubSearch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
